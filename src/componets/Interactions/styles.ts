import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  list: {
    alignItems: 'center',
    paddingHorizontal: 18,
    paddingTop: 12

  },
  title: {
    color: '#fff',
    fontSize: 10,
    padding: 4
  }
});